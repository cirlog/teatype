// This is a placeholder - the actual config is in vite.config.ts
// ESM modules need proper exports. Since we can't easily share .ts files,
// each app should duplicate the vite config or use CommonJS require
export default {};
